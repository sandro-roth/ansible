- name: system setup | apt | fix APT sources for Ubuntu 24.04 (noble)
  copy:
    src: corsair/apt_source_list
    dest: /etc/apt/sources.list
    owner: root
    group: root
    mode: 0644

- name: system setup | apt | update APT package index
  apt:
    update_cache: yes
