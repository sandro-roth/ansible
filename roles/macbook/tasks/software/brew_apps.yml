---
- block:

  - name: macbook | brew | update & upgrade all packages
    community.general.homebrew:
      update_homebrew: true
      upgrade_all: true
    when: brew_upgrade | default(false)

  - name: macbook | brew | ansible
    community.general.homebrew:
      name: ansible
      state: latest

  become: false