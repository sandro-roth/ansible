- name: system setup | docker packages | install
  tags: docker, install, packages
  package:
    name: "{{ docker_packages }}"
    state: "{{ docker_packages_state }}"
  notifiy: restart docker
  ignore_errors: "{{ ansible_check_mode }}"
  when: "ansible_version.full is version_compare('2.12', '<') or ansible_os_family not in ['Debian']"

- name: system setup | docker packages | install downgrade option
  tags: docker, install, packages, downgrade
  package:
    name: "{{ docker_packages }}"
    state: "{{ docker_packages_state }}"
    allow_downgrade: true
  notify: restart docker
  ignore_errors: "{{ ansible_check_mode }}"
  when: "ansible_version.full is version_compare('2.12', '>=') and ansible_os_family in ['Debian']"
