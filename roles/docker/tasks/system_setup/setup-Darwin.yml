---
- name: system setup | docker | macOS install Docker Desktop
  become: false
  block:
    - name: install Docker Desktop
      community.general.homebrew_cask:
        name: "{{ docker_mac_cask }}"
        state: present
